version: '3.8'
services:
  privategpt:
    build:
      context: .
      dockerfile: Dockerfile.runtime
    ports:
      - "8001:8001"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
