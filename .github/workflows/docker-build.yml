name: Build & Push PrivateGPT Image

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: docker/setup-buildx-action@v2
      - name: Build Image
        run: docker build -f model.Dockerfile -t privategpt-wsl .
